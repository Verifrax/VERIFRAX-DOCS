# Contributing (VERIFRAX-DOCS)

Public documentation portal.

Rules:
- keep URLs stable (no link rot)
- deterministic builds (pin dependencies if used)
- doc changes must reference SPEC anchors when normative
- no secrets/tokens in examples
